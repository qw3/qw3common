$('#combo_cidade').html("<%= escape_javascript select( "#{params[:tipo]}[endereco_attributes]", "cidade_id", @cidades.all.collect{ |c| [c.nome, c.id] }, {:prompt => '-- Selecione uma cidade --'} ) %>");
